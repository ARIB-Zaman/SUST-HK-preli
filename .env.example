PORT=3000
GEMINI_API_KEY=your_google_ai_studio_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# Retry config (optional — these are the defaults)
GEMINI_MAX_RETRIES=4       # total extra attempts after the first failure
GEMINI_BASE_DELAY_MS=500   # base wait before first retry (doubles each time)
